name: 📦 Build Atop Linux Packages (.deb & .rpm)

on:
  workflow_dispatch:
    inputs:
      atop_version:
        description: "Version of atop to build"
        required: true
        default: "2.12.0"

env:
  ATOP_VERSION: ${{ github.event.inputs.atop_version }}

jobs:
  build-deb:
    name: 🏗️ Build .deb packages
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [amd64, arm64]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU for cross-compilation
        uses: docker/setup-qemu-action@v3

      - name: Build .deb for atop ${{ env.ATOP_VERSION }} (${{ matrix.arch }})
        run: |
          docker buildx build \
            --platform linux/${{ matrix.arch }} \
            --build-arg ARCH=${{ matrix.arch }} \
            --build-arg ATOP_VERSION=${{ env.ATOP_VERSION }} \
            --output type=local,dest=build-deb/${{ matrix.arch }} \
            -f Dockerfile.deb .

      - name: Upload .deb artifact (${{ matrix.arch }})
        uses: actions/upload-artifact@v4
        with:
          name: atop-deb-${{ matrix.arch }}-${{ env.ATOP_VERSION }}
          path: build-deb/${{ matrix.arch }}/*.deb

  build-rpm:
    name: 🏗️ Building .rpm packages
    runs-on: ubuntu-latest
    strategy:
      matrix:
        el: [el8, el9]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build .rpm file for atop ${{ env.ATOP_VERSION }} (${{ matrix.el }})
        run: |
          docker build --build-arg ATOP_VERSION=$ATOP_VERSION -f Dockerfile_${{ matrix.el }}.rpm -t atop-${{ matrix.el }}-builder .
          container_id=$(docker create atop-${{ matrix.el }}-builder)
          mkdir -p build-rpm-${{ matrix.el }}
          docker cp "$container_id":/build/. ./build-rpm-${{ matrix.el }}
          docker rm "$container_id"

      - name: Upload .rpm artifact (${{ matrix.el }})
        uses: actions/upload-artifact@v4
        with:
          name: atop-rpm-${{ matrix.el }}-${{ env.ATOP_VERSION }}
          path: build-rpm-${{ matrix.el }}/
